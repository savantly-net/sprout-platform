logging:
  level:
    org.flywaydb: DEBUG
sprout:
  cors:
    allowed-methods: GET, OPTIONS, PUT, POST, PATCH, DELETE
  security:
    authorization:
      public-paths:
      - /**
    authentication:
      basic:
        users:
        - username: admin
          password: changeme!
          email-address: test@savantly.net
          roles:
          - ADMIN
