logging:
  level:
    org.springframework.security.web.authentication: DEBUG
sprout:
  jpa:
    use-embedded-db: true
  security:
    authentication:
      basic:
        users:
        - username: test
          password: test
          email-address: test@savantly.net
          roles:
          - ADMIN
          - NEW_ROLE
          