logging:
  level:
    org.springframework.security.web.authentication: DEBUG
sprout:
  security:
    authentication:
      basic:
        users:
        - username: admin
          password: test
          email-address: test@savantly.net
          roles:
          - ADMIN
          