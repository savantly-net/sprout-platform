logging:
  level:
    org.springframework.security.web: DEBUG
sprout:
  jpa:
    use-embedded-db: true
  security:
    authentication:
      basic:
        users:
        - username: admin
          password: admin
          email-address: test@savantly.net
          roles:
          - ADMIN
          