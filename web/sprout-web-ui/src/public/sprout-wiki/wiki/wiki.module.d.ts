import { SproutPlugin, SproutPluginRegistryService } from '@savantly/ngx-sprout-plugin';
export declare class WikiModule implements SproutPlugin {
    name: string;
    template: any;
    constructor(registry: SproutPluginRegistryService);
}
