import React, { PropsWithChildren } from 'react';
import { TimeZoneInfo } from '@savantly/sprout-api';
interface Props {
    info?: TimeZoneInfo;
}
export declare const TimeZoneDescription: React.FC<PropsWithChildren<Props>>;
export {};
